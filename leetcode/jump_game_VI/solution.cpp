#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    int maxResult(vector<int>& nums, int k) {
        size_t last_index = nums.size();
        
    }
};

int main () {
    Solution s = Solution();
    vector<int> nums = {1,-1,-2,4,  -7,3};
    s.maxResult(nums, 2);
    return 0;
}