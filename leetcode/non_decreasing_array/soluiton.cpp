#include <iostream>

using std::vector;

class Solution {
public:
  bool checkPossibility(vector<int> &nums) {
    for (int i = 0; i < nums.size(); i++) {
      std::cout << nums[i] << std::endl;
    }
  }
};

int main() {
  Solution s = Solution();
  s.checkPossibility({1, 2, 3};

  )
}